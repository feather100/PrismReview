# 交接文档（2026-08-04，新对话接续用）

> **背景：** 上一对话推进 Sprint 11.0 至 T12 + Demo + CI，本文件让新对话无缝接续。
> **必读：** `docs/coordination/ACTIVE_SPRINT.md`、`docs/coordination/Sprint_11.0_Summary.md`、`docs/research/phase3-task-list-20260803.md`
> **更新（2026-08-04 EOD）：** 本日已完成交接项 #1（demo 验证 + 根因修复）与 #2（README 更新），最新状态见 [`Handoff_20260804_EOD.md`](Handoff_20260804_EOD.md)。

---

## 1. 当前状态（提交到哪）

- **main = edc5f48**（已推送）：T1–T12 + Demo 修复（startReview/expandRoles）+ CI pnpm 修复
- **未提交但已验证的改动（本次交接前必须 commit）**：
  - `reviews.service.ts`：**interrupt/resume 委托 orchestrator**（原为 stub，只改 status → 中断后评审卡死）
  - `review-orchestrator.ts`：**resume 末尾补 checkMeetingComplete**（中断时全部 turn 已终态，重派发被幂等跳过 → 卡 running）
  - `moderator.ts`：**决策链重排**（risk_gate/escalate_to_human 优先于 maxRounds force_stop——否则到顶 abort 而非转人工）
  - 测试：moderator.decide.spec / convergence-signals.spec 补 confidenceScore
  - **migrations 入库**（.gitignore 移除 + 21 文件）；CI 用 `migrate deploy`；Deployment_Migration_Checklist 风险项更新
- 验证：tsc 0 error / jest **208/208**（提交前已跑绿）

## 2. 本次会话遗留的未完成项（新对话接着做）

1. ✅ **research demo 全链路验证（已完成，2026-08-04 续）**：`DEMO_MODE=research node scripts/demo-run.js` 全链路已验证 → **终态 completed**（review `62b567dc`：r1 advance → r2 escalate 扩容 → r3 risk_gate → interrupted → 自动 resume → completed；DB status=completed，scoring pass + finalize 均跑）。
   **本次发现并修复的根因**（原 checkMeetingComplete 方案无效）：
   - `review-orchestrator.resume()`：中断时当前轮 turn 已全部终态 → 重派发幂等跳过，且 `checkMeetingComplete` 入队的 `meeting.complete.{id}.r{round}` 会被 queue 进程内 `processedIds` 幂等拦截（该轮已处理过一次）→ `handleTurnsComplete` 永不触发 → 卡 running。修复：resume 先判定当前轮是否已全部终态，是则直接走 `handleTurnsComplete`（跳过重派发）。
   - `moderator.decide()`：人工放行（humanGateApproved=true）后，round>=2 未收敛 + 已扩容一次会再次 `escalate_to_human` → 死循环。修复：新增 `humanGateApproved → converge` 分支。
   - 测试：`review-orchestrator.resume.spec.ts`（新，2 例）+ `moderator.decide.spec` 补 demo resume 场景；jest **211/211**，tsc 0。
   - 备注：报告页 `report.verdict` 为既有启发式（有 high-risk 即 conditionally_approved，不按阈值），与 `scoring.verdict`（按阈值 32→rejected）不一致——既有设计，本次未改。
2. **README 更新 + GitHub 页面装饰**：README 还停留在 P5 状态（严重过时）。计划：更新到 Sprint 11.0 现状 + 用 `docs/demo/report-screenshot.png`（企业评审报告截图，已入库）装饰。research 报告截图可补。
3. **gold standard 人工收集（D）**：需用户提供 5 份脱敏方案文档 + 3 位专家 → `node scripts/gold-standard-kappa.js` 跑 κ → 评分可信度 ρ/MAE 白皮书。
4. **前端切片 D**（可选）：成本看板（T6）+ 校准页（T10）+ risk_gate 中断原因提示。

## 3. 关键背景速览

- **Sprint 11.0 完成**：T1–T12（生命周期/收敛信号/评分纪律/评分分离/降级/成本/升级/风险门/段落锚点/校准/并发/辩论上下文）
- **Demo 已跑通**：LongCat 真 LLM，4 角色 → 报告页新 UI（加权评分卡 + 段落跳转）；research 模式已验证 T7 扩容 + T8 风险门（r3 risk_gate → interrupted）
- **CI**：pnpm 版本冲突已修；smoke 用 `migrate deploy`（migrations 已入库）
- **数据库**：19 迁移已应用；demo 服务可用 `node scripts/demo-run.js --cleanup` 清理

## 4. 环境/工具备忘

- 数据库：`docker compose up -d postgres`（当前可能已停）
- API 启动冒烟：`node scripts/e2e-smoke.js`（5/5）；Sprint 10.1 verify：`node scripts/e2e-verify-sprint-10.1.js`（5/5）
- 真 LLM demo：`DEMO_MODE=research|enterprise node scripts/demo-run.js`（LongCat provider 在 DB，LLM 调用较慢 ~30s/轮）
- 浏览器查看：in-app browser（`agent.browsers.getForUrl`），截图存 `docs/demo/`
- **坑**：仓库文件 CRLF，行级编辑最稳；node_repl 里 prisma client 会缓存旧 schema（改 schema 后需重启 node_repl 或 `prisma generate` + 新进程）